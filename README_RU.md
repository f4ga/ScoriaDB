<div align="center">
  <img src="https://capsule-render.vercel.app/api?type=waving&color=gradient&customColorList=12&height=200&section=header&text=🪨%20ScoriaDB&fontSize=70&fontAlignY=40&animation=fadeIn" alt="Header">
  <img src="https://capsule-render.vercel.app/api?type=rect&color=gradient&customColorList=1&height=60&section=header&text=🔥%20Встраиваемая%20LSM-база%20данных%20для%20Go%20|%20Крепкая%20как%20камень%2C%20лёгкая%20как%20пепел&fontSize=20&fontAlignY=50&animation=twinkling" alt="Subtitle">
  <br><br>

  [![CI](https://github.com/f4ga/ScoriaDB/actions/workflows/ci.yml/badge.svg)](https://github.com/f4ga/ScoriaDB/actions/workflows/ci.yml)
  [![Go Version](https://img.shields.io/badge/Go-1.23+-00ADD8?logo=go)](https://go.dev/)
  [![License](https://img.shields.io/badge/license-Apache%202.0-blue)](LICENSE)

  <div align="center">

  [🇬🇧 English](README.md) &nbsp;&nbsp;|&nbsp;&nbsp; [🇷🇺 Русский](README_RU.md)

  </div>

  <br>
  <table align="center" style="font-size: 1.4em; line-height: 2;">
    <tr><td>📖</td><td><a href="#-что-такое-scoriadb">Что такое ScoriaDB</a></td></tr>
    <tr><td>👥</td><td><a href="#-для-кого">Для кого</a></td></tr>
    <tr><td>✨</td><td><a href="#-почему-scoriadb">Почему ScoriaDB</a></td></tr>
    <tr><td>📊</td><td><a href="#-бенчмарки">Бенчмарки</a></td></tr>
    <tr><td>📊</td><td><a href="#-сравнение-с-redis">Сравнение с Redis</a></td></tr>
    <tr><td>🧩</td><td><a href="#-возможности-и-функции">Возможности и функции</a></td></tr>
    <tr><td>🛡️</td><td><a href="#-надёжность-и-восстановление">Надёжность и восстановление</a></td></tr>
    <tr><td>🕰️</td><td><a href="#-как-работает-mvcc">Как работает MVCC</a></td></tr>
    <tr><td>🚀</td><td><a href="#-быстрый-старт">Быстрый старт</a></td></tr>
    <tr><td>📈</td><td><a href="#-прогресс-mvp">Прогресс MVP</a></td></tr>
    <tr><td>📁</td><td><a href="#-структура-проекта">Структура проекта</a></td></tr>
    <tr><td>🗺️</td><td><a href="#-дорожная-карта">Дорожная карта</a></td></tr>
    <tr><td>📄</td><td><a href="#-лицензия">Лицензия</a></td></tr>
    <tr><td>❓</td><td><a href="#-faq">FAQ</a></td></tr>
    <tr><td>🤝</td><td><a href="#-поддержать-проект">Поддержать проект</a></td></tr>
  </table>
</div>

---

## 📖 Что такое ScoriaDB?

**ScoriaDB** — это встраиваемая key‑value база данных, которая заменит использование связок из сетевого и встраиваемого хранилища.
- **Как встраиваемая библиотека** — компилируется прямо в ваш Go‑процесс, давая максимальную скорость без внешних зависимостей.
- **Как сервер** — предоставляет встроенные gRPC, CLI и (запланированный) Web UI, не требуя ни строчки дополнительного кода.


> Проект сейчас на стадии **MVP**. Все ключевые компоненты — LSM‑движок, MVCC, ACID‑транзакции, Column Families, gRPC/REST API, CLI и Docker — реализованы и протестированы. Сейчас разработывается демо‑инструмент Scorix для анализа логов.

---

## 👥 Для кого

ScoriaDB полезна:

- **Go‑разработчикам**, которые хотят добавить персистентное KV‑хранилище в свой микросервис, как бибилотеку , CLI‑утилиту или агент — без лишней инфраструктуры.
- **IoT и Edge‑инженерам** — когда нужно локальное хранилище на устройстве, но при этом важен удалённый доступ и мониторинг.
- **Разработчикам на Python, Java, C++ и др языках** — доступ через gRPC для любого, поддерживающего gRPC, языка.
- **Командам с микросервисной архитектурой** — один сервер, много клиентов на разных языках, единый API.
- **Всем, кому нужен простой персистентный KV‑движок**, который разворачивается за секунды с помощью Docker и предлагает встроенные интерфейсы CLI и REST/gRPC.

---

## ✨ Почему ScoriaDB?

| Преимущество | Что оно даёт |
| :--- | :--- |
| **Встраиваемость** | Чистый Go, без cgo, один `import` — мощный LSM‑движок внутри вашего процесса. |
| **Готовый сервер** | gRPC, REST, CLI (и Web UI в планах) доступны сразу, без написания сетевого кода. |
| **ACID‑транзакции** | Snapshot Isolation, интерактивные транзакции, атомарный WriteBatch. |
| **Column Families** | Независимые LSM‑деревья с настройками компакшена для каждого CF и атомарностью между ними. |
| **MVCC** | Читатели никогда не блокируют писателей; чтение снапшотов и (будущие) запросы Time Travel. |
| **Кросс‑языковой доступ** | 12+ языков через gRPC — Python, Java, C++, Rust, Node.js, C# и другие. |
| **Надёжность** | WAL + Manifest с fsync — восстановление после краха без потери данных. |
| **Производительность** | ~150 нс на чтение (MemTable), ~1 мкс на запись, zero‑copy Value Log для больших значений. |

---

## 📊 Бенчмарки

Измерено на Intel Core i3‑1215U (8 потоков), Go 1.23+, Linux amd64.  
Запуск: `go test -bench=. ./internal/engine ./pkg/scoria`

| Операция | Размер значения | Время (нс/оп) | Пропускная способность |
|---|---|---|---|
| `Put` (маленькое) | < 64 байт | **1 070 нс** | ~ 935 000 оп/с |
| `Put` (большое) | 4 КБ (Value Log) | **4 785 нс** | ~ 209 000 оп/с |
| `Get` (попадание) | ключ в MemTable | **~150 нс** | ~ 6 600 000 оп/с |
| `ScoriaPut` | через публичный API | **1 063 нс** | накладные расходы < 1 % |
| `ScoriaGet` | через публичный API | **144 нс** | накладные расходы ~ 5 % |

> Чтение на уровне BoltDB (~100–200 нс), но с MVCC, транзакциями и конкурентной записью.  
> Запись — около 1 млн оп/с для одиночных `Put` без батчинга.  
> Накладные расходы публичного API минимальны — интерфейс `DB` практически прозрачен.

---

## 📊 Сравнение с Redis

ScoriaDB **не** является прямой заменой Redis. Redis — это удалённая, in‑memory платформа данных; ScoriaDB — встраиваемая, дисково‑ориентированная база.  
Таблица ниже показывает **локальные бенчмарки** (без сетевых накладных расходов) и **сетевые бенчмарки** Redis, чтобы проиллюстрировать архитектурную разницу.

| Характеристика | ScoriaDB (локально) | Redis CE (сетевые) |
| :--- | :--- | :--- |
| **Развёртывание** | Встраиваемая библиотека или отдельный сервер | Отдельный серверный процесс |
| **Сетевые накладные расходы** | Нет (embedded‑режим) | TCP (0,1–0,2 мс+) |
| **Задержка чтения (Get)** | ~150 нс | ~0,24–0,31 мс  |
| **Задержка записи (Set)** | ~1 070 нс | ~0,45 мс  |
| **Персистентность данных** | Полная (WAL, Manifest, fsync) | Опциональная (RDB, AOF) |
| **Транзакции** | ACID, Snapshot Isolation | Нет (pipelining) |
| **Поддержка языков** | gRPC (12+ языков) | Нативный протокол (клиентские библиотеки) |

YCSB бенчмарки ScoriaDB будут опубликованы во время релиза 2
---

## 🧩 Что может предложить ScoriaDB

### Storage Engine
| Функция | Описание | Статус |
| :--- | :--- | :--- |
| **LSM‑дерево** | Отсортированная MemTable (B‑tree) с периодическим сбросом в SSTable на диск. | ✅ |
| **WAL (Write‑Ahead Log)** | Каждая операция журналируется с контрольной суммой CRC32 перед попаданием в MemTable. | ✅ |
| **Value Log (WiscKey)** | Значения > 64 байт выносятся в отдельный append‑only файл; mmap для zero‑copy чтения. | ✅ |
| **SSTable** | Блочный индекс, префиксное сжатие ключей, фильтр Блума, диапазонный фильтр (min/max ключ). | ✅ |
| **Leveled Compaction** | Фоновое слияние SSTable для освобождения места и удаления tombstone. | ✅ |
| **Сжатие** | Snappy и Zstd на уровне блоков SSTable. | ✅ |
| **GC Value Log** | Сборщик мусора для Value Log. | ⚒️ В разработке|
| **TTL для записей** | Автоматическое удаление записей. | ⏳ Релиз 2 |
| **Снапшот‑бэкап** | Мгновенные снапшоты через hardlink. | ⏳ Релиз 2 |

### Транзакции и версионность
| Функция | Описание | Статус |
| :--- | :--- | :--- |
| **MVCC** | Многоверсионное конкурентное управление с инвертированными временными метками. | ✅ |
| **Snapshot Isolation** | Чтение видит консистентный снапшот на `startTS`; писатели никогда не блокируют читателей. | ✅ |
| **Интерактивные транзакции** | `Begin()` → `Get`/`Put`/`Delete` → `Commit()`/`Rollback()` с оптимистичной блокировкой. | ✅ |
| **WriteBatch** | Атомарная группа операций под единым `commitTS`. | ✅ |
| **Обнаружение конфликтов** | При коммите проверяется, не был ли ключ изменён после `startTS`. | ✅ |

### Данные и организация
| Функция | Описание | Статус |
| :--- | :--- | :--- |
| **Column Families** | Независимые LSM‑деревья с настройками компакшена для каждого CF. Атомарные записи между CF. | ✅ |
| **Вторичные индексы** | Автоматическое поддержание индексов по префиксам ключей. | ⏳ Релиз 2 |
| **Sorted Sets** | Нативная поддержка сортированных множеств (как в Redis). | ⏳ Релиз 2 |
| **Lists / очереди** | Надёжные очереди FIFO/LIFO с атомарными операциями. | ⏳ Релиз 2 |
| **JSON‑документы** | Автоматическая сериализация и индексация JSON. | ⏳ Релиз 2 |

### Сетевые интерфейсы и инструменты
| Функция | Описание | Статус |
| :--- | :--- | :--- |
| **Embedded Go API** | Интерфейсы `DB` и `CFDB` для прямого встраивания. | ✅ |
| **gRPC API** | Все CRUD‑операции, стриминг Scan, транзакции. | ✅ |
| **REST API + WebSocket** | HTTP‑эндпоинты для Web UI, push‑уведомления об изменениях ключей. | ✅ |
| **CLI‑клиент (`scoria`)** | Команды: `get`, `set`, `del`, `scan`, `txn`, `admin`, `inspect`, интерактивная оболочка. | ✅ |
| **Web UI (React)** | Дашборд для просмотра данных и управления пользователями. |⚒️ В разработке  |
| **Аутентификация** | JWT, bcrypt, роли `admin`/`readwrite`/`readonly`. | ✅ |
| **Docker и docker‑compose** | Многоэтапные образы сервера и CLI, запуск одной командой. | ✅ |
| **CI/CD** | GitHub Actions: линтер, тесты с race‑детектором, проверка заголовков лицензии. | ✅ |

---

## 🛡️ Надёжность и восстановление

**Manifest** — это журнал метаданных, который записывает каждое изменение в наборе файлов с атомарным `fsync`. **WAL** сохраняет каждую операцию с CRC32. При старте движок читает Manifest, чтобы восстановить точную файловую структуру, и WAL, чтобы восстановить незафиксированные записи.

Вместе они гарантируют **полное восстановление после внезапного отключения питания** — без повреждения метаданных и без потери подтверждённых записей.

---

## 🕰️ Как работает MVCC

**MVCC (Multi‑Version Concurrency Control)** означает, что каждая запись создаёт новую версию ключа вместо перезаписи старой. Каждая версия несёт временную метку (`commitTS`).

1. При `Put` создаётся новая версия ключа с `commitTS = <текущее время>`.
2. Когда транзакция вызывает `Begin()`, она получает `startTS` — снапшот на этот момент.
3. Все чтения внутри транзакции видят только версии с `commitTS ≤ startTS`.
4. При `Commit()` движок проверяет, не был ли ключ изменён после `startTS` (обнаружение конфликтов).

**Почему это важно:**
- **Писатели никогда не блокируют читателей.**
- **Snapshot Isolation.** Консистентный взгляд на данные даже при конкурентной записи.
- **Time Travel станет возможным** (запланирован на Релиз 2).

---

## 🚀 Быстрый старт

### Docker (рекомендуется)

```bash
git clone https://github.com/f4ga/scoriadb.git && cd scoriadb
docker compose -f deployments/docker-compose.yml up --build
```

Запускает сервер (gRPC :50051, HTTP :8080) и выполняет интеграционный тест CLI.

### Локальная сборка

```bash
git clone https://github.com/f4ga/scoriadb.git && cd scoriadb
go build -o scoria-server ./cmd/server
go build -o scoria-cli ./cmd/cli

./scoria-server &   # сервер на :50051 и :8080

# Получить токен (при первом старте создаётся admin/admin)
TOKEN=$(./scoria-cli --addr localhost:50051 admin auth admin admin)

# CRUD
./scoria-cli --addr localhost:50051 --token "$TOKEN" set hello world
./scoria-cli --addr localhost:50051 --token "$TOKEN" get hello
```

### Запуск тестов (c гонкой) и бенчмарков

```bash
go test -race ./...
go test -bench=. ./internal/engine ./pkg/scoria
```

---

## 📈 Прогресс MVP

Все запланированные функции MVP реализованы и протестированы:

| Категория | Компонент | Статус |
| :--- | :--- | :--- |
| **Ядро** | LSM‑дерево, Value Log, WAL | ✅ Готово |
| | SSTable (Bloom, диапазонный фильтр) | ✅ Готово |
| | Manifest, VFS, Compaction | ✅ Готово |
| | базовый GC Value Log | ⚒️ В разработке |
| **Версионность** | MVCC (Snapshot Isolation) | ✅ Готово |
| **Транзакции** | WriteBatch, Интерактивные транзакции | ⚒️ Фикшу баги |
| **Данные** | Column Families | ✅ Готово |
| **API** | Embedded Go, gRPC, REST, WebSocket | ✅ Готово |
| **Интерфейсы** | CLI‑клиент (`scoria`) | ✅ Готово |
| | Web UI (React) | ⚒️ В разработке |
| **Безопасность** | Аутентификация (JWT, роли) | ✅ Готово |
| **DevOps** | Docker, docker‑compose | ✅ Готово |
| **Качество** | CI, бенчмарки, линтинг | ✅ Готово |

---

## 📁 Структура проекта

```
scoriadb/
├── cmd/
│   ├── server/              # Точка входа gRPC + HTTP сервера
│   └── cli/                 # CLI клиент (scoria)
├── internal/
│   ├── engine/              # LSM ядро: MemTable, SSTable, VLog, WAL, Manifest, compaction
│   │   ├── sstable/         # Чтение/запись SSTable, Bloom, кодирование
│   │   ├── vfs/             # Абстракция файловой системы (тестируемый слой)
│   │   └── tests/           # Интеграционные тесты движка
│   ├── mvcc/                # Кодирование MVCC‑ключей с инвертированными метками
│   ├── txn/                 # Транзакции (WriteBatch, интерактивные)
│   ├── cf/                  # Реестр Column Families + batch
│   └── api/
│       ├── grpc/            # Реализация gRPC сервера
│       ├── rest/            # REST API сервер
│       └── ws/              # WebSocket хаб
├── pkg/scoria/              # Публичный Embedded Go API (DB, CFDB)
│   └── tests/               # API‑уровневые интеграционные тесты
├── proto/                   # Protobuf описание сервиса
├── scoriadb/                # Сгенерированный Go код из protobuf
├── tests/                   # E2E и интеграционные тесты
├── web/                     # React фронтенд 
└── deployments/             # Docker и docker‑compose
```

---

## 🗺️ Дорожная карта

### 🟢 Релиз 1 (MVP) — СКОРО  
- [x] LSM движок, MVCC, транзакции, Column Families
- [ ] атомарный WriteBatch в WAL 🆕 
- [ ] базовый GC Value Log 🆕 
- [x] Embedded API, gRPC, REST, WebSocket
- [x] CLI И Web клиент, Docker, CI/CD
- [ ] Демо‑инструмент Scorix: импорт логов, поиск, агрегация, живой просмотр. 🆕 
- [ ] Документация на Github Pages 
r
### 🟡 В будущем 
- **Готовность к проду:** продвинутый GC, TTL, бэкап/восстановление, lock‑free MemTable.
- **Нативные структуры данных:** Sorted Sets, Lists, очереди, вторичные индексы, JSON‑документы. 🆕 
- **Распределённость:** Raft‑репликация и федерация без Raft, шардирование, распределённые ACID‑транзакции (2PC).
- **Безопасность:** mTLS, Row‑Level Security, аудит.
- **Наблюдаемость:** Time Travel запросы, Chaos Monkey, OpenTelemetry.


---

## 📄 Лицензия

ScoriaDB распространяется под лицензией **Apache License 2.0**.  
Полный текст лицензии находится в файле [LICENSE](LICENSE).  

Дополнительные условия:  
- Уведомления о сторонних библиотеках перечислены в [NOTICE](NOTICE).  
- Вклад в проект регулируется [Contributor License Agreement](CONTRIBUTING.md).  
- Все исходные файлы содержат обязательные заголовки лицензии; проверка выполняется автоматически в CI.  

---

## ❓ FAQ

### 1. ScoriaDB — это библиотека или сервер?
**И то, и другое.** Вы можете встроить её как Go‑библиотеку (`import "github.com/f4ga/scoriadb/pkg/scoria"`) или запустить как отдельный сервер с gRPC, REST и CLI.

### 2. Чем она отличается от BoltDB?
BoltDB — B+‑дерево, один писатель, нет сети. ScoriaDB — LSM, конкурентная запись, MVCC, встроенные gRPC/CLI.

### 3. Чем она отличается от BadgerDB?
Обе используют WiscKey. BadgerDB — только библиотека, без сервера, без интерактивных транзакций. ScoriaDB добавляет Column Families, MVCC, gRPC и CLI.

### 4. Чем она отличается от RocksDB?
RocksDB — C++ (cgo), нет встроенного сервера. ScoriaDB — чистый Go, с собственным gRPC/CLI и интерактивными ACID‑транзакциями.

### 5. Могу ли я использовать ScoriaDB из Python/Java/C++?
Да, через gRPC API. Для встраивания нужен Go; для всех остальных языков — сетевой доступ.

### 6. Поддерживает ли ScoriaDB SQL?
Нет. ScoriaDB — это KV‑база данных. SQL‑подобные запросы не планируются.

### 7. Что происходит с данными при внезапном отключении питания?
WAL + Manifest с fsync гарантируют полное восстановление без повреждения метаданных и без потери данных.

### 8. Готова ли она к продакшену сегодня?
Стадия MVP: ядро, транзакции, API и CLI работают, скоро будет базовый GC . Для критичных систем мы рекомендуем дождаться Релиза 2 ( бэкапы, TTL, Разные типы данных, Raft, 2PC).

### 9. Какие структуры данных сейчас поддерживаются?
Только строки. Релиз 2 представит Sorted Sets, Lists, очереди и индексы.

### 10. Будет ли распределённый режим без Raft?
Да, в Релизе 2: шардирование и федерация без обязательного Raft.

### 11. Почему gRPC, а не REST?
gRPC быстрее (HTTP/2, бинарный protobuf) и поддерживает стриминг. REST используется как дополнительный интерфейс для Web UI и отладки.

### 12. Как я могу помочь проекту?
⭐ Поставьте звезду на GitHub, сообщайте об ошибках в Issues, отправляйте Pull Request'ы и делитесь проектом.

---

## 🤝 Поддержать проект

ScoriaDB — свободное программное обеспечение. Вы можете помочь:

- **⭐️ Поставив звезду** на GitHub.
- **🐛 Сообщая об ошибках** через [Issues](https://github.com/f4ga/scoriadb/issues).
- **💻 Отправляя pull request'ы** — любые улучшения приветствуются.
- **📣 Рассказывая** о проекте в соцсетях или чатах.

<div align="center">
  <i>Крепкая как камень. Лёгкая как пепел.</i><br><br>
  <img src="https://capsule-render.vercel.app/api?type=waving&color=gradient&customColorList=12&height=120&section=footer">
</div>